<%= form_for(@titulo) do |f| %>
  <% if @titulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@titulo.errors.count, "error") %> prohibited this titulo from being saved:</h2>

      <ul>
      <% @titulo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
